#ifndef UTILS_INTEGER_HHH
#define UTILS_INTEGER_HHH

#include <stdint.h>

uint32_t round_up_u32(uint32_t input_value, uint32_t modular);

uint16_t round_up_u16(uint16_t input_value, uint16_t modular);

#endif

